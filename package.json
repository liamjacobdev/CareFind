{
  "name": "carefind-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Unit + e2e tests for the CareFind frontend logic (carefind.logic.js) and page.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.49.1",
    "@vitest/coverage-v8": "^2.1.8",
    "esbuild": "0.21.5",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
